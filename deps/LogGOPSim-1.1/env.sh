#!/bin/sh

CURRENT_PATH=$(cd $(dirname ${BASH_SOURCE[0]}); pwd)

export LD_LIBRARY_PATH=$CURRENT_PATH:$LD_LIBRARY_PATH
export LIBRARY_PATH=$CURRENT_PATH:$LIBRARY_PATH
export PATH=$CURRENT_PATH:$PATH
export CPATH=$CURRENT_PATH:$CPATH
